# Start here - v65

Use this complete repository to replace v64.

The release contains two targeted corrections:

1. **Generic entity lock** - the company/domain/external-source controls apply to every company. SHEIN remains a regression benchmark, not a special-case implementation.
2. **Two-page PDF geometry** - claim risk labels and nested content stay within card boundaries.

Read `V65_CHANGELOG.md`, deploy using `DEPLOY_V65.txt`, and review the PDF in `PREVIEW_V65/`.
