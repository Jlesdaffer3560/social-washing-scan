--- /mnt/data/Durably_v61_Logical_2_Page_Report_Structure/frontend.html	2026-07-15 14:32:27.000000000 +0000
+++ /mnt/data/Durably_v62_Professional_Claims_and_Readable_Report/frontend.html	2026-07-15 16:52:41.092872330 +0000
@@ -33,7 +33,42 @@
     .section h2::before{content:'';display:inline-block;width:4px;height:20px;border-radius:2px;background:var(--accent2);vertical-align:middle}
     .card.section{border-top:3px solid var(--accent2)}
     @media(max-width:850px){.grid,.cols,.scores,.redflag-grid{grid-template-columns:1fr}.hero h1{font-size:28px}}
-</style>
+
+    /* v62: compact, structured claim-signal presentation */
+    .claim-overview{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin:12px 0 18px}
+    .claim-overview .metric{background:#f7f9fc;border:1px solid var(--line);border-radius:11px;padding:10px 12px}
+    .claim-overview .metric strong{display:block;font-size:20px;line-height:1;color:var(--accent2)}
+    .claim-overview .metric span{display:block;font-size:11.5px;color:var(--muted);margin-top:4px}
+    .claim-framework-note{font-size:12.5px;color:var(--muted);background:#f7f9fc;border:1px solid var(--line);border-radius:10px;padding:10px 12px;margin-bottom:12px}
+    .claim-group-heading{justify-content:space-between;margin-top:18px;margin-bottom:8px}
+    .claim-group-heading.social-heading{background:var(--warn-soft);color:#68470f;border-left:5px solid var(--warn)}
+    .claim-group-heading .group-count{font-size:12px;font-weight:700;opacity:.8}
+    .claim-subheading{margin-top:12px}
+    .claim-card.compact{padding:0;margin:8px 0;border-left-width:4px;overflow:hidden;box-shadow:none}
+    .claim-card.compact>.claim-head{padding:11px 13px;margin:0;align-items:center;background:#fff}
+    .claim-card.compact[open]>.claim-head{background:#f8fafc;border-bottom:1px solid var(--line)}
+    .claim-card.compact .claim-title{font-size:14px;line-height:1.25}
+    .claim-summary-line{font-size:12.3px;color:var(--muted);margin-top:4px;display:flex;gap:6px;flex-wrap:wrap;align-items:center}
+    .claim-gap{font-size:12px;color:#4d5d6a;margin-top:5px}
+    .claim-gap strong{color:var(--teal-dark)}
+    .claim-detail-body{padding:12px 14px}
+    .claim-occurrence{border:1px solid var(--line);border-radius:10px;padding:10px;margin:8px 0;background:#fffdf7}
+    .claim-occurrence-head{display:flex;justify-content:space-between;gap:10px;align-items:flex-start;margin-bottom:6px}
+    .claim-source-short{font-size:11.5px;color:#516073;overflow-wrap:anywhere}
+    .claim-badges{display:flex;gap:5px;flex-wrap:wrap;justify-content:flex-end}
+    .badge{display:inline-block;border:1px solid #ccd8e4;background:#eef3f8;color:#173f5f;border-radius:999px;padding:2px 7px;font-size:10.5px;font-weight:700}
+    .badge.reg{background:#edf7f0;border-color:#bfe2c8;color:#205b31}
+    .badge.audience{background:#fff8ea;border-color:#ead8aa;color:#68470f}
+    .claim-detail-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:9px}
+    .claim-detail-box{background:#f7f9fc;border:1px solid var(--line);border-radius:9px;padding:9px;font-size:12.3px}
+    .claim-recommendation{background:#eef6f5;border:1px solid #cddfdd;border-radius:9px;padding:9px;margin-top:9px;font-size:12.3px}
+    .show-more-claims{border:1px dashed #b9c6d2;border-radius:10px;margin-top:8px;background:#fbfcfe}
+    .show-more-claims>summary{cursor:pointer;padding:9px 12px;color:var(--accent2);font-weight:700;font-size:12.5px}
+    .show-more-claims>div{padding:0 10px 8px}
+    .view-details{font-size:11px;color:var(--accent);font-weight:700;white-space:nowrap}
+    @media(max-width:760px){.claim-overview{grid-template-columns:1fr 1fr}.claim-detail-grid{grid-template-columns:1fr}}
+
+  </style>
 </head>
 <body>
 <div class="wrap">
@@ -108,7 +143,7 @@
 
     <div class="card section">
       <h2>Key sustainability claim signals</h2>
-      <p class="muted">This scan is built around two EU legal lenses. EmpCo (Directive (EU) 2024/825) is primarily an environmental-claims law, with a narrower, secondary basis for social claims (Article 6(1)(b)); social signals under EmpCo are therefore limited to the most material, high-severity items. The EU Forced Labour Regulation (Regulation (EU) 2024/3015) covers forced-labour and supply-chain claims specifically. Claims that fit neither lens are shown separately. The table shows the exact wording detected in the reviewed material; potentially problematic words are highlighted in <strong>bold yellow</strong>, followed by a short list of the main trigger terms.</p>
+      <p class="muted">Claims are grouped by regulatory relevance and claim theme. The classification indicates which legal or substantiation lens may be relevant; it is not a legal finding. Each row shows the key wording, source, audience and main evidence gap. Open a row to review the exact passage and recommended improvement.</p>
       <div id="claimSignals"></div>
     </div>
 
@@ -197,56 +232,60 @@
 function termsInText(text){let low=String(text||'').toLowerCase();return PROBLEM_TERMS.filter(t=>low.includes(t)).slice(0,10)}
 function highlightTerms(text, explicit){let html=esc(text||'');let terms=arr(explicit).concat(termsInText(text)).filter(Boolean);terms=[...new Set(terms.map(x=>String(x).trim()).filter(x=>x.length>1))].sort((a,b)=>b.length-a.length).slice(0,12);for(let term of terms){let safe=esc(term).replace(/[.*+?^${}()|[\]\\]/g,'\\$&');try{html=html.replace(new RegExp('('+safe+')','ig'),'<strong>$1</strong>')}catch(e){}}return {html,terms};}
 function isMaterialClaim(x){let t=String(x.claim_type||x.type||'').toLowerCase();return !(t.startsWith('no material')||t.startsWith('no major'))}
-// v57m: EmpCo's own operative provisions are overwhelmingly environmental (Annex I points
-// 2a/4a/4b/4c/10a, Art. 6(2)(d), Art. 7(7) are all environmental-claim rules) -- the social
-// angle is real but thin, resting on a single general clause (Art. 6(1)(b)) plus Recital 3's
-// short list of examples (wages, safety, human rights, equal treatment, gender equality,
-// inclusion, diversity). So EmpCo splits into two sub-groups under one heading: Environmental
-// (the full, well-developed claim set) and Social (flat, no further sub-headings, and limited
-// to the small set of types that map directly onto Recital 3's named characteristics, filtered
-// to High risk only so the section stays focused on the most material items rather than every
-// borderline signal). The Forced Labour Regulation section is kept strict: only the claim type
-// that is actually about forced labour / modern slavery falls under that Regulation -- general
-// "responsible sourcing" or "supplier" claims are not specific to it and move to "Other".
-const EMPCO_SOCIAL_TYPES=new Set(['aspirational or future social-performance claim',
-  'human-rights or labour-rights claim','human-rights / labour-rights claim',
-  'health, safety or worker-welfare claim']);
+const EMPCO_SOCIAL_TYPES=new Set(['aspirational or future social-performance claim','human-rights or labour-rights claim','human-rights / labour-rights claim','health, safety or worker-welfare claim','diversity or inclusion claim','customer fairness or accessibility claim']);
 const FORCED_LABOUR_TYPES=new Set(['forced-labour product or supply-chain claim']);
-function claimRegulatoryBucket(claimType, dimension){
-  let t=String(claimType||'').toLowerCase().trim();
-  let dim=String(dimension||'').toLowerCase();
-  if(FORCED_LABOUR_TYPES.has(t))return 'forced_labour';
-  if(dim==='green')return 'empco_environmental';
-  if(EMPCO_SOCIAL_TYPES.has(t))return 'empco_social';
+function claimRegulatoryBucket(claimType,dimension){
+  let t=String(claimType||'').toLowerCase().trim(),dim=String(dimension||'').toLowerCase();
+  if(FORCED_LABOUR_TYPES.has(t)||t.includes('forced-labour')||t.includes('forced labor')||t.includes('modern slavery'))return 'forced_labour';
+  if(dim.includes('green'))return 'empco_environmental';
+  if(EMPCO_SOCIAL_TYPES.has(t)||t.includes('human-right')||t.includes('labour-right')||t.includes('labor-right')||t.includes('worker-welfare')||t.includes('diversity')||t.includes('inclusion'))return 'social_characteristics';
   return 'other';
 }
-function isHighRisk(x){return String(x.risk_level||x.risk||'').toLowerCase()==='high';}
-function renderClaims(d){let all=arr(d.claim_inventory).length?arr(d.claim_inventory):[...arr(d.green_findings),...arr(d.social_findings),...arr(d.findings)];all=all.filter(isMaterialClaim);
-  let envir=all.filter(x=>claimRegulatoryBucket(x.claim_type||x.type,x.dimension)==='empco_environmental');
-  let socialAll=all.filter(x=>claimRegulatoryBucket(x.claim_type||x.type,x.dimension)==='empco_social');
-  let social=socialAll.filter(isHighRisk);
-  let forcedLabour=all.filter(x=>claimRegulatoryBucket(x.claim_type||x.type,x.dimension)==='forced_labour');
-  let other=[...all.filter(x=>claimRegulatoryBucket(x.claim_type||x.type,x.dimension)==='other'),...socialAll.filter(x=>!isHighRisk(x))];
-  let empcoBlock=`<h3 class="claim-group-heading empco-heading">&#127793; EmpCo Directive (EU 2024/825)</h3>`
-    +`<h4 class="claim-subheading">Environmental claims</h4>${envir.length?claimCards(envir):'<p class="empty">No material environmental claim signal was retained.</p>'}`
-    +`<h4 class="claim-subheading">Social claims</h4>${social.length?claimCards(social):'<p class="empty">No material, high-severity social claim signal was retained.</p>'}`;
-  let forcedLabourBlock=`<h3 class="claim-group-heading forcedlabour-heading">&#9878; EU Forced Labour Regulation (EU 2024/3015)</h3>${forcedLabour.length?claimCards(forcedLabour):'<p class="empty">No material forced-labour or supply-chain assurance wording was retained.</p>'}`;
-  let blocks=[empcoBlock, forcedLabourBlock];
-  if(other.length)blocks.push(`<h3 class="claim-group-heading other-heading">Other sustainability or social claims</h3>${claimCards(other)}`);
-  $('claimSignals').innerHTML=blocks.join('');}
+function claimSubcategory(type,bucket){let t=String(type||'').toLowerCase();
+  if(bucket==='empco_environmental'){
+    if(t.includes('climate')||t.includes('carbon')||t.includes('offset'))return 'Climate and carbon claims';
+    if(t.includes('label')||t.includes('certif'))return 'Sustainability labels and certification';
+    if(t.includes('future'))return 'Future environmental-performance claims';
+    if(t.includes('compar')||t.includes('circular')||t.includes('recycl'))return 'Comparative and circularity claims';
+    if(t.includes('generic'))return 'Generic environmental claims';
+    return 'Other environmental claims';
+  }
+  if(bucket==='social_characteristics'){
+    if(t.includes('human')||t.includes('labour')||t.includes('labor'))return 'Human and labour rights';
+    if(t.includes('welfare')||t.includes('safety')||t.includes('health'))return 'Worker welfare and safety';
+    if(t.includes('divers')||t.includes('inclusion')||t.includes('equal'))return 'Diversity and inclusion';
+    if(t.includes('customer')||t.includes('accessib')||t.includes('fair'))return 'Customer fairness and accessibility';
+    return 'Other social characteristics';
+  }
+  if(bucket==='forced_labour'){
+    if(t.includes('traceab'))return 'Product traceability';
+    if(t.includes('supplier')||t.includes('sourcing'))return 'Supplier and sourcing assurances';
+    return 'Forced-labour and supply-chain assurance';
+  }
+  return 'Other sustainability claims';
+}
+function riskRank(x){let r=String(x.risk_level||x.risk||'').toLowerCase();return r.includes('very')?4:r.includes('high')?3:(r.includes('medium')||r.includes('elev'))?2:1}
+function claimAudience(x){return String(x.audience_lens||x.audience_group||'Audience not classified').replace(/_/g,' ')}
+function isConsumerFacing(x){let a=claimAudience(x).toLowerCase();return a.includes('consumer')||a.includes('b2c')||a.includes('marketing')||a.includes('product')}
+function evidenceGap(x){let e=x.evidence_needed;if(Array.isArray(e)&&e.length)return e.slice(0,4).join('; ');if(typeof e==='string'&&e.trim())return e;return 'Scope, methodology, evidence period, verification basis and limitations.'}
+function suggestedImprovement(x){return x.suggested_rewrite||x.rewrite||'Use precise, claim-specific wording and disclose scope, methodology, evidence, reporting period and limitations.'}
+function clusterClaims(rows){let m=new Map();for(let x of arr(rows).filter(isMaterialClaim)){let title=String(x.claim_type||x.type||'Claim signal').trim();let key=title.toLowerCase();if(!m.has(key))m.set(key,{title,items:[],representative:x});let c=m.get(key);c.items.push(x);if(riskRank(x)>riskRank(c.representative)||(riskRank(x)===riskRank(c.representative)&&Number(x.claim_score||0)>Number(c.representative.claim_score||0)))c.representative=x;}return [...m.values()].sort((a,b)=>riskRank(b.representative)-riskRank(a.representative)||Number(b.representative.claim_score||0)-Number(a.representative.claim_score||0));}
+function claimSourceCompact(r){let label=r.source_label||r.source_url||'Reviewed material';return String(label).replace(/^https?:\/\//,'').replace(/\/$/,'').slice(0,105)}
+function occurrenceHtml(r){let text=String(r.claim_text||r.claim||'').replace(/\s+/g,' ').trim();let h=highlightTerms(text,arr(r.problematic_terms).concat([r.matched_phrase]));let audience=claimAudience(r);return `<div class="claim-occurrence"><div class="claim-occurrence-head"><div class="claim-source-short"><strong>${esc(claimSourceCompact(r))}</strong></div><div class="claim-badges"><span class="badge audience">${esc(audience)}</span>${isConsumerFacing(r)?'<span class="badge reg">Consumer-facing</span>':''}</div></div><div class="quote" translate="no">${h.html||'<span class="empty">No exact passage available.</span>'}</div></div>`}
+function clusterCard(cluster,bucket){let r=cluster.representative;let dim=String(r.dimension||r.claim_dimension||'').toLowerCase().includes('green')?'green':'social';let sources=[...new Set(cluster.items.map(claimSourceCompact))];let trigger=String(r.matched_phrase||arr(r.problematic_terms)[0]||'').trim();let snippet=String(r.claim_text||r.claim||'').replace(/\s+/g,' ').trim();if(snippet.length>115)snippet=snippet.slice(0,112)+'…';let count=cluster.items.length;let regLabel=bucket==='empco_environmental'?'EmpCo environmental':bucket==='social_characteristics'?'Social characteristics':bucket==='forced_labour'?'Forced-labour readiness':'Other claim';return `<details class="claim-card compact ${dim}"><summary class="claim-head"><div style="min-width:0"><div class="claim-title">${esc(cluster.title)}${count>1?` <span class="small">· ${count} occurrences</span>`:''}</div><div class="claim-summary-line"><span class="badge reg">${esc(regLabel)}</span><span>${esc(snippet)}</span><span>· ${esc(sources.slice(0,2).join('; '))}</span></div><div class="claim-gap"><strong>Main gap:</strong> ${esc(evidenceGap(r))}</div></div><div style="text-align:right"><span class="risk ${riskClass(r.risk_level||r.risk)}">${esc(r.risk_level||r.risk||'Review')}</span><div class="view-details">View details</div></div></summary><div class="claim-detail-body">${cluster.items.map(occurrenceHtml).join('')}<div class="claim-detail-grid"><div class="claim-detail-box"><strong>Why it matters</strong><br>${esc(r.why_flagged||r.risk_reason||r.issue||r.analysis||'The wording requires claim-specific substantiation and audience review.')}</div><div class="claim-detail-box"><strong>Evidence required</strong><br>${esc(evidenceGap(r))}</div></div><div class="claim-recommendation"><strong>Recommended improvement</strong><br>${esc(suggestedImprovement(r))}</div></div></details>`}
+function groupedClaimsHtml(clusters,bucket){if(!clusters.length)return '<p class="empty">No material claim signal was retained in this category.</p>';let groups={};clusters.forEach(c=>{let sub=claimSubcategory(c.title,bucket);(groups[sub]||(groups[sub]=[])).push(c)});let ordered=Object.entries(groups);let all=ordered.flatMap(([sub,cs])=>cs.map(c=>({sub,c})));let visible=all.slice(0,5),rest=all.slice(5);function render(items){let last='',html='';for(let {sub,c} of items){if(sub!==last){html+=`<h4 class="claim-subheading">${esc(sub)}</h4>`;last=sub}html+=clusterCard(c,bucket)}return html}let html=render(visible);if(rest.length)html+=`<details class="show-more-claims"><summary>Show ${rest.length} more claim group${rest.length===1?'':'s'}</summary><div>${render(rest)}</div></details>`;return html}
+function renderClaims(d){let all=arr(d.claim_inventory).length?arr(d.claim_inventory):[...arr(d.green_findings),...arr(d.social_findings),...arr(d.findings)];all=all.filter(isMaterialClaim);let clusters=clusterClaims(all);let by={empco_environmental:[],social_characteristics:[],forced_labour:[],other:[]};clusters.forEach(c=>{let r=c.representative;by[claimRegulatoryBucket(c.title,r.dimension||r.claim_dimension)].push(c)});let high=all.filter(x=>riskRank(x)>=3).length,medium=all.filter(x=>riskRank(x)===2).length,consumer=all.filter(isConsumerFacing).length;let overview=`<div class="claim-overview"><div class="metric"><strong>${all.length}</strong><span>material occurrences</span></div><div class="metric"><strong>${high}</strong><span>high-risk signals</span></div><div class="metric"><strong>${medium}</strong><span>medium-risk signals</span></div><div class="metric"><strong>${consumer}</strong><span>consumer-facing</span></div></div><div class="claim-framework-note">The categories indicate the most relevant assessment lens. They do not constitute a legal conclusion or finding of non-compliance.</div>`;function section(cls,title,bucket,note){let n=by[bucket].reduce((a,c)=>a+c.items.length,0);return `<h3 class="claim-group-heading ${cls}"><span>${title}</span><span class="group-count">${n} signal${n===1?'':'s'}</span></h3>${note?`<div class="small" style="margin:0 4px 8px">${note}</div>`:''}${groupedClaimsHtml(by[bucket],bucket)}`}
+  $('claimSignals').innerHTML=overview
+    +section('empco-heading','1. EmpCo — Environmental claims','empco_environmental','Consumer-facing environmental wording, labels, comparisons and future-performance claims.')
+    +section('social-heading','2. Social characteristics','social_characteristics','Potentially relevant under general misleading-practice rules. High and medium signals remain in this section.')
+    +section('forcedlabour-heading','3. Forced labour and supply-chain assurance','forced_labour','Regulatory-readiness and substantiation lens — not a finding of non-compliance.')
+    +section('other-heading','4. Other sustainability claims','other','Material claims without a sufficiently direct link to the two primary regulatory lenses.');}
 function claimSource(r){let label=r.source_label||r.source_url||'Reviewed material';let url=r.source_url||'';let lens=r.audience_lens||r.audience_group||'';let link=url && /^https?:/i.test(url)?`<a href="${esc(url)}" target="_blank" rel="noopener">${esc(label)}</a>`:esc(label);return `<div>${link}</div>${lens?`<div class="small">${esc(lens)}</div>`:''}`}
-function claimCards(rows){rows=arr(rows).filter(isMaterialClaim);if(!rows.length)return '<p class="empty">No material problematic claim signal retained in this category.</p>';
-  // v57s: a quick-scan "at a glance" list up top -- type, risk, one-line snippet -- so a reader
-  // can see what was found before scrolling through the full detail cards below. Nothing is
-  // removed from the full cards; this is purely additive for scanability on longer result sets.
-  let glance=rows.map(r=>{let snippet=String(r.claim_text||r.claim||'').replace(/\s+/g,' ').trim();if(snippet.length>90)snippet=snippet.slice(0,87)+'…';return `<div class="glance-row"><span class="risk ${riskClass(r.risk_level||r.risk)}">${esc(r.risk_level||r.risk||'')}</span><span class="glance-type">${esc(r.claim_type||r.type||'Claim signal')}</span><span class="glance-snippet">${esc(snippet)}</span></div>`}).join('');
-  let glanceBlock=rows.length>1?`<div class="glance-list">${glance}</div>`:'';
-  let cards=rows.map((r,i)=>{let text=String(r.claim_text||r.claim||'').replace(/\s+/g,' ').trim();let h=highlightTerms(text,arr(r.problematic_terms).concat([r.matched_phrase]));let dim=String(r.dimension||r.claim_dimension||'').toLowerCase().includes('green')?'green':'social';let terms=h.terms.length?h.terms.map(t=>`<span class="pill problem">${esc(t)}</span>`).join(' '):'<span class="small">The full wording pattern implies a specific high-sensitivity sustainability claim.</span>';let source=claimSource(r);let why=[r.why_flagged||r.risk_reason||r.issue||r.analysis||''];if(r.regulatory_signal&&!why[0].includes(r.regulatory_signal))why.push(r.regulatory_signal);let spec=r.specification_check&&r.specification_check.comment;if(spec)why.push('In this passage: '+spec);return `<details class="claim-card ${dim}" open><summary class="claim-head"><div><div class="claim-title">${esc(r.claim_type||r.type||'Claim signal')}</div><div class="small">Problematic signal ${i+1} · ${esc(r.audience_lens||r.audience_group||'Audience not classified')}</div></div><span class="risk ${riskClass(r.risk_level||r.risk)}">${esc(r.risk_level||r.risk||'')}</span></summary><div class="source-ref"><div class="claim-step">1. Source page or document</div>${source}${r.source_interpretation?`<div class="small">${esc(r.source_interpretation)}</div>`:''}</div><div class="detected-passage"><div class="claim-step">2. Exact claim passage retained for review</div><div class="quote" translate="no">${h.html||'<span class="empty">No exact passage available.</span>'}</div></div><div class="trigger-row"><div class="claim-step">3. Specific trigger phrase(s) within the passage</div>${terms}</div><div class="subtlebox"><strong>Why this was retained:</strong> ${why.map(w=>esc(w)).join(' ')}</div><div class="subtlebox"><strong>Suggested improvement:</strong> ${esc(r.suggested_rewrite||r.rewrite||'')}</div></details>`;}).join('');
-  return glanceBlock+cards}
+
 function renderRedFlags(d){let by=d.red_flags_by_dimension||{};let green=arr(by.green);let social=arr(by.social);let flags=arr(d.stakeholder_red_flags);let regulatory=[];let external=[];for(let f of flags){let s=String(typeof f==='string'?f:JSON.stringify(f));let l=s.toLowerCase();if(l.includes('empco')||l.includes('regulation')||l.includes('forced labour')||l.includes('forced labor'))regulatory.push(s);if(l.includes('external')||l.includes('public-source')||l.includes('contradict'))external.push(s)}
 function isUrgent(s){let l=s.toLowerCase();return l.includes('high')||l.includes('empco')||l.includes('forced labour')||l.includes('forced labor')||l.includes('regulation')}
 function box(cls,title,items){items=arr(items).filter(Boolean);return `<div class="redflag ${cls}"><h3>${esc(title)}</h3><ul>${items.length?items.slice(0,6).map(x=>isUrgent(x)?`<li class="urgent">&#9888; ${esc(x)}</li>`:`<li>${esc(x)}</li>`).join(''):'<li>No specific red flag retained.</li>'}</ul></div>`}$('redflags').innerHTML=`<div class="redflag-grid">${box('green-flag','Green-claim red flags',green)}${box('social-flag','Social-claim red flags',social)}${box('reg-flag','Regulatory red flags',regulatory)}${box('', 'Negative external-context red flags', external)}</div>`;}
-function renderExternal(d){let g=arr(d.external_research?.green?.targeted_negative_sources);let s=arr(d.external_research?.social?.targeted_negative_sources);let legacy=arr(d.external_research?.targeted_negative_sources);let retained=g.length+s.length+legacy.length;function srcList(title,items){items=arr(items);if(!items.length)return '';return `<h3>${esc(title)}</h3>`+items.map(src=>`<div class="source"><strong>${esc(src.title||'External stakeholder signal')}</strong><br><a href="${esc(src.url||'#')}" target="_blank" rel="noopener">${esc(src.url||'')}</a><p class="small">${esc(src.category||'Negative external stakeholder source')} · ${esc(src.credibility||'')}</p><p>${esc(src.content||'')}</p></div>`).join('')}let html='<div class="stakeholder-note"><strong>Scope:</strong> this section only retains negative external stakeholder perceptions, such as criticism, allegations, complaints, investigations, enforcement, litigation, NGO/union/regulator signals or clearly negative press. Company-owned documents, policies, reports, corporate news, awards, partnerships and positive or neutral announcements are excluded.</div>';html+=srcList('Green-claim related negative external stakeholder signals',g);html+=srcList('Social-claim related negative external stakeholder signals',s);html+=srcList('Other retained negative external stakeholder signals',legacy);let msg=retained?'<p class="small">Only retained negative stakeholder signals are shown.</p>':'<p class="empty">No negative external stakeholder signal was retained after excluding company-owned documents and positive or neutral news. If external search is not configured, no live external-source check is performed.</p>';$('external').innerHTML=html+msg;}
+function renderExternal(d){let g=arr(d.external_research?.green?.targeted_negative_sources),sigs=arr(d.external_research?.social?.targeted_negative_sources),legacy=arr(d.external_research?.targeted_negative_sources);let all=[...g,...sigs,...legacy];let seen=new Set();all=all.filter(x=>{let k=x.url||x.title;if(!k||seen.has(k))return false;seen.add(k);return true});function card(src){let url=src.url||'',source=src.source_name||String(url).replace(/^https?:\/\//,'').split('/')[0]||src.category||'External source';return `<div class="source" style="border:1px solid var(--line);border-left:4px solid var(--danger);border-radius:11px;padding:12px;margin:9px 0;background:#fff"><div style="display:flex;justify-content:space-between;gap:12px"><strong>${esc(src.title||'External stakeholder signal')}</strong><span class="badge">${esc(src.review_status||'Retained — manual verification required')}</span></div><div class="claim-summary-line"><span>${esc(source)}</span><span>· ${esc(src.published_date||'Date not available')}</span><span>· ${esc(src.status||'Status unclear')}</span><span>· ${esc(src.relevance||'Medium')} relevance</span></div>${src.content?`<p style="margin:7px 0 4px">${esc(src.content)}</p>`:''}<div class="small"><strong>Related claim area:</strong> ${esc(src.related_claim_area||src.dimension||'Sustainability claims')} · <strong>Entity match:</strong> ${esc(src.entity_match||'Direct')}</div>${url?`<a href="${esc(url)}" target="_blank" rel="noopener">Open source</a>`:''}</div>`}let note='<div class="stakeholder-note"><strong>External context:</strong> candidate search results are filtered for company ownership, entity match, negative polarity and green/social relevance. Only retained signals are shown. They require manual verification before use.</div>';$('external').innerHTML=note+(all.length?all.slice(0,6).map(card).join(''):'<p class="empty">No negative external public-source signal was retained. If external search was unavailable, this does not confirm an absence of relevant external criticism or enforcement.</p>');}
 function renderActions(d){let actions=arr(d.company_action_plan);$('actions').innerHTML=actions.length?actions.map(a=>{let p=String(a.priority||'');let urgent=/priority\s*1\b/i.test(p);return `<div class="action-card${urgent?' urgent-action':''}"><strong class="${urgent?'urgent-priority':''}">${urgent?'&#9888; ':''}${esc(p)}</strong> ${esc(a.title||'')}<br><span class="muted">${esc(a.action||'')}</span></div>`}).join(''):'<p class="empty">No actions available.</p>'}
 function miniSource(r){return (r.source_label||r.source_url||'Reviewed material').replace(/\s+/g,' ').slice(0,90)}
 async function downloadCompanyReportPdf(){
