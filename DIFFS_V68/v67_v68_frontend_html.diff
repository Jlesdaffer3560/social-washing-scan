--- /mnt/data/_v67_extract/Durably_v67_Scan_Coverage_Source_Register/frontend.html	2026-07-16 14:06:43.000000000 +0000
+++ /mnt/data/Durably_v68_Stability_Methodology_Privacy/frontend.html	2026-07-16 17:15:07.312144195 +0000
@@ -15,7 +15,7 @@
     .kicker{font-size:12px;text-transform:uppercase;letter-spacing:.12em;opacity:.9;font-weight:700}.hero h1{margin:8px 0 10px;font-size:34px;line-height:1.15}.hero p{max-width:940px;margin:0;color:#e7f0f3;font-size:16px}.version{margin-top:16px;font-size:12px;color:#d7e6eb}
     .grid{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:20px}.card{background:var(--card);border:1px solid var(--line);border-radius:var(--radius);padding:20px;box-shadow:var(--shadow)}
     h2{font-size:21px;margin:0 0 12px} h3{font-size:16px;margin:0 0 10px}.muted{color:var(--muted)} .small{font-size:12.5px;color:var(--muted)}
-    label{font-weight:700;display:block;margin-bottom:8px} input[type=url]{width:100%;border:1.5px solid var(--line);border-radius:10px;padding:12px;background:#fff;font:inherit;transition:border-color .15s} input[type=url]:focus{border-color:var(--accent);outline:none;box-shadow:0 0 0 3px rgba(38,95,92,.1)}.file-native{display:none}.file-row{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.file-name{font-size:13px;color:var(--muted);background:#fff;border:1px solid var(--line);border-radius:10px;padding:10px 12px;min-width:180px}.btn{border:0;border-radius:10px;background:var(--accent);color:#fff;padding:10px 14px;font-weight:700;cursor:pointer;font-size:14px;text-decoration:none;display:inline-block}.btn.secondary{background:#eef3f8;color:#173f5f;border:1px solid #ccd8e4}.btn.primary-outline{background:#fff;color:#173f5f;border:1px solid #9cb3c8}.btn:disabled{opacity:.55;cursor:not-allowed}.notice{margin-top:14px;padding:11px 12px;border-radius:10px;background:#eef6f5;border:1px solid #cddfdd;color:#244744}.warning{background:#fff8ea;border-color:#ead8aa;color:#68470f}.error{background:#fff0f0;border-color:#e2baba;color:#842424}.hidden{display:none!important}
+    label{font-weight:700;display:block;margin-bottom:8px} input[type=text],input[type=url]{width:100%;border:1.5px solid var(--line);border-radius:10px;padding:12px;background:#fff;font:inherit;transition:border-color .15s} input[type=text]:focus,input[type=url]:focus{border-color:var(--accent);outline:none;box-shadow:0 0 0 3px rgba(38,95,92,.1)}.file-native{display:none}.file-row{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.file-name{font-size:13px;color:var(--muted);background:#fff;border:1px solid var(--line);border-radius:10px;padding:10px 12px;min-width:180px}.btn{border:0;border-radius:10px;background:var(--accent);color:#fff;padding:10px 14px;font-weight:700;cursor:pointer;font-size:14px;text-decoration:none;display:inline-block}.btn.secondary{background:#eef3f8;color:#173f5f;border:1px solid #ccd8e4}.btn.primary-outline{background:#fff;color:#173f5f;border:1px solid #9cb3c8}.btn:disabled{opacity:.55;cursor:not-allowed}.btn:focus-visible,input:focus-visible,summary:focus-visible,a:focus-visible{outline:3px solid rgba(38,95,92,.28);outline-offset:2px}.notice{margin-top:14px;padding:11px 12px;border-radius:10px;background:#eef6f5;border:1px solid #cddfdd;color:#244744}.warning{background:#fff8ea;border-color:#ead8aa;color:#68470f}.error{background:#fff0f0;border-color:#e2baba;color:#842424}.hidden{display:none!important}
     .purpose{margin-top:20px}.purpose ol{margin:10px 0 0;padding-left:20px}.purpose li{margin:5px 0}.how-it-works summary{cursor:pointer;font-weight:700;color:var(--accent-dark, #173f5f);padding:8px 0;list-style:none;display:flex;align-items:center;gap:6px}.how-it-works summary::-webkit-details-marker{display:none}.how-it-works summary::before{content:"▸";transition:transform .15s}.how-it-works[open] summary::before{transform:rotate(90deg)}.how-it-works summary:hover{text-decoration:underline}.pill{display:inline-block;padding:4px 8px;border-radius:999px;background:#eef3f8;border:1px solid var(--line);font-size:12px;margin:2px 4px 2px 0;color:#334}.pill.problem{background:#fff1f1;border-color:#efb8b8;color:#842424;font-weight:700}.pill.green{background:#edf7f0;border-color:#bfe2c8;color:#205b31}.pill.blue{background:#eef3f8;border-color:#ccd8e4;color:#173f5f}
     .scores{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.score{border:1px solid var(--line);border-radius:16px;padding:18px;background:#fbfcfe;position:relative;overflow:hidden}.score:before{content:'';position:absolute;left:0;top:0;bottom:0;width:6px;background:#9cb3c8}.score.global:before{background:#173f5f}.score.green-score:before{background:#276749}.score.social-score:before{background:#9b6a17}.score .num{font-size:38px;font-weight:850;letter-spacing:-.03em}.score .label{font-size:13px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:#46576b}.score .risk{font-weight:850;margin-top:4px}.risk.high,.risk.very-high{color:var(--danger)}.risk.medium,.risk.elevated{color:var(--warn)}.risk.low{color:var(--ok)}.score-bar{height:5px;background:#e5eaf2;border-radius:3px;margin-top:10px;overflow:hidden}.score-bar-fill{height:5px;border-radius:3px;transition:width .4s ease}.global .score-bar-fill{background:#173f5f}.green-score .score-bar-fill{background:#276749}.social-score .score-bar-fill{background:#9b6a17}.score-interp{font-size:11.5px;color:var(--muted);margin-top:5px}.score.entity-context{grid-column:1/-1;background:#fafbfd;border-style:dashed}.score.entity-context:before{background:#9cb3c8}.score.entity-context .num{font-size:22px}
     table{width:100%;border-collapse:collapse;margin-top:8px} th,td{padding:10px;border-bottom:1px solid var(--line);vertical-align:top;text-align:left} th{font-size:12px;text-transform:uppercase;letter-spacing:.04em;color:#516073;background:#f7f9fc} td{font-size:13.5px} .quote{color:#243449}.quote strong{background:#fff1a8;padding:1px 2px;border-radius:3px;color:#111827}.section{margin-top:20px}.cols{display:grid;grid-template-columns:1fr 1fr;gap:18px}.list{margin:8px 0 0;padding-left:18px}.list li{margin:6px 0}.toolbar{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.download-panel{background:#f8fbff;border:1px solid #cbd9e8;border-radius:12px;padding:14px;margin-top:14px}.download-panel h3{margin-bottom:5px}.reportbox{white-space:pre-wrap;background:#0f172a;color:#e5edf7;border-radius:12px;padding:16px;max-height:420px;overflow:auto;font-family:Consolas,monospace;font-size:12.5px}.source a{color:#174e78}.footer{margin-top:26px;color:var(--muted);font-size:12.5px}.empty{color:var(--muted);font-style:italic}.spinner{width:16px;height:16px;border-radius:50%;border:2px solid #c7d4df;border-top-color:var(--accent);animation:spin 1s linear infinite;display:inline-block;vertical-align:middle}@keyframes spin{to{transform:rotate(360deg)}}
@@ -69,6 +69,11 @@
     .view-details{font-size:11px;color:var(--accent);font-weight:700;white-space:nowrap}
     @media(max-width:760px){.claim-overview{grid-template-columns:1fr 1fr}.claim-detail-grid{grid-template-columns:1fr}}
 
+  
+    .privacy-box{margin-top:10px;padding:10px 12px;border:1px solid #cbd9e8;background:#f8fbff;border-radius:10px;font-size:12.5px;color:#33495d}
+    .result-nav{position:sticky;top:0;z-index:20;display:flex;gap:7px;flex-wrap:wrap;margin:14px 0;padding:9px;background:rgba(246,248,251,.96);border:1px solid var(--line);border-radius:12px;backdrop-filter:blur(5px)}
+    .result-nav a{font-size:12px;font-weight:750;color:var(--accent2);text-decoration:none;padding:5px 8px;border-radius:8px;background:#fff;border:1px solid var(--line)}
+    .status-badge{display:inline-block;padding:2px 6px;border-radius:999px;font-size:10.5px;font-weight:700;background:#edf7f0;color:#205b31;border:1px solid #bfe2c8}.status-badge.partial{background:#fff8ea;color:#68470f;border-color:#ead8aa}.status-badge.limited{background:#fff1f1;color:#842424;border-color:#efb8b8}
   </style>
 </head>
 <body>
@@ -77,14 +82,14 @@
     <div class="kicker">EmpCo &middot; EU Forced Labour Regulation lenses</div>
     <h1>Sustainability Scan</h1>
     <p>Assess whether public sustainability claims are specific, substantiated, audience-appropriate and consistent with relevant public information. The scan applies two regulatory lenses: <strong>EmpCo</strong> &mdash; Directive (EU) 2024/825, officially the "Empowering Consumers for the Green Transition" Directive &mdash; for consumer-facing environmental (green) claims, and the <strong>EU Forced Labour Regulation</strong> (Regulation (EU) 2024/3015) for product, supplier and supply-chain claims related to forced labour.</p>
-    <div id="version" class="version">Checking backend version...</div>
+    <div id="version" class="version">Build {{APP_RELEASE_LABEL}} · {{APP_RELEASE_DATE}} · {{APP_VERSION}}</div>
   </section>
 
   <section class="card purpose">
     <p class="muted" style="margin-bottom:10px">Assess sustainability claims for wording risk, evidence gaps, regulatory sensitivity and relevant public contradictions &mdash; not a broad ESG rating.</p>
     <details class="how-it-works">
       <summary>How the assessment works</summary>
-      <p class="muted" style="margin-top:10px">This is a claims-risk scan, not a broad ESG rating. It identifies sustainability wording that may be too broad, insufficiently evidenced, consumer-facing under EmpCo, exposed to the EU Forced Labour Regulation, or potentially contradicted by relevant external public-source context.</p>
+      <p class="muted" style="margin-top:10px">This is a claims-risk scan, not a broad ESG rating. It identifies sustainability wording that may be too broad, insufficiently evidenced, consumer-facing under EmpCo, may create forced-labour-related assurance risk, or may be contradicted by relevant external public-source context.</p>
       <ol>
         <li><strong>Claim wording:</strong> broad, generic, absolute, future-oriented, comparative or label/icon-based claims.</li>
         <li><strong>Evidence gap:</strong> missing scope, methodology, KPIs, limits, verification, grievance/remedy, audit coverage, traceability or due-diligence evidence.</li>
@@ -101,7 +106,7 @@
         <h2>Website scan &mdash; public sources</h2>
         <p class="muted">Enter a company name or website (e.g. <strong>Inditex</strong>, <strong>inditex.com</strong>). The scan finds the official site and reviews its public pages &mdash; sustainability, policies, product/campaign pages, press and investor pages &mdash; plus public external signals (media, NGO, regulator, union) when configured. Only public material is checked.</p>
         <label for="url">Company name or website</label>
-        <input id="url" type="text" placeholder="e.g. Inditex, inditex.com or www.inditex.com" />
+        <input id="url" type="text" autocomplete="organization" spellcheck="false" placeholder="e.g. Inditex, inditex.com or www.inditex.com" />
         <div class="toolbar"><button id="runUrl" class="btn">Run website scan</button></div>
         <p class="small" style="margin-top:8px;color:var(--muted)">Public sources only. If a bare company name cannot be confidently matched to an official domain, the scan reports how it resolved the name so you can verify it.</p>
       </div>
@@ -112,16 +117,16 @@
         <input id="file" class="file-native" type="file" accept=".txt,.html,.htm,.md,.csv,.docx,.pdf" />
         <div class="file-row"><button id="chooseFile" type="button" class="btn secondary">Choose file</button><span id="fileName" class="file-name">No file selected</span></div>
         <label for="docCompanyName" style="margin-top:8px">Company name (optional, recommended)</label>
-        <input id="docCompanyName" type="text" placeholder="e.g. Acme Corp" />
-        <p class="small">Supported: TXT, HTML, MD, CSV, DOCX. PDF extraction is best-effort.</p>
+        <input id="docCompanyName" type="text" autocomplete="organization" placeholder="e.g. Acme Corp" />
+        <p class="small">Supported: TXT, HTML, MD, CSV, DOCX. PDF extraction is best-effort.</p><div class="privacy-box"><strong>Document privacy:</strong> the application processes the uploaded file for this request and does not intentionally write its contents to persistent application storage. The file is not combined with website crawling or external search. Do not upload personal data, legally privileged information or material that your organisation does not permit you to process through this hosted service. Hosting infrastructure may retain limited request metadata according to its own settings.</div>
         <div class="toolbar"><button id="runDoc" class="btn">Upload and scan document</button></div>
       </div>
     </div>
-    <div id="status" class="notice">Ready.</div>
+    <div id="status" class="notice" role="status" aria-live="polite">Ready.</div>
   </section>
 
-  <section id="results" class="hidden">
-    <div class="card section">
+  <section id="results" class="hidden"><nav class="result-nav" aria-label="Result sections"><a href="#summary-section">Summary</a><a href="#coverage-section">Coverage</a><a href="#claims-section">Claims</a><a href="#external-section">External signals</a><a href="#actions-section">Actions</a></nav>
+    <div class="card section" id="summary-section">
       <h2>Executive summary</h2>
       <p id="resolutionNote" class="notice warning hidden" style="margin-bottom:12px"></p>
       <p id="reliabilityWarning" class="notice warning hidden" style="margin-bottom:12px;background:#fff4e5;border:1px solid #e0a020;color:#7a4a00;padding:10px 14px;border-radius:8px;font-weight:600"></p>
@@ -136,9 +141,9 @@
       </div>
     </div>
 
-    <div class="card section">
+    <div class="card section" id="coverage-section">
       <h2>Scan coverage and reviewed sources</h2>
-      <p class="muted">A transparent register of the website pages and documents that were successfully reviewed. Failed access attempts are listed separately and were not analysed as evidence.</p>
+      <p class="muted">A transparent register of retrieved website pages and documents, showing whether each source was fully analysed, partially analysed or yielded limited text. Failed access attempts are listed separately and were not analysed as evidence.</p>
       <div id="scanCoverage"></div>
       <div class="coverage-toolbar"><button class="btn secondary" type="button" onclick="downloadSourceRegisterCsv()">Download source register (CSV)</button></div>
     </div>
@@ -149,18 +154,18 @@
       <div id="drivers" class="cols"></div>
     </div>
 
-    <div class="card section">
+    <div class="card section" id="claims-section">
       <h2>Key sustainability claim signals</h2>
       <p class="muted">Claims are grouped by regulatory relevance and claim theme. The classification indicates which legal or substantiation lens may be relevant; it is not a legal finding. Each row shows the key wording, source, audience and main evidence gap. Open a row to review the exact passage and recommended improvement.</p>
       <div id="claimSignals"></div>
     </div>
 
-    <div class="card section">
+    <div class="card section" id="actions-section">
       <h2>Recommended actions</h2>
       <div id="actions"></div>
     </div>
 
-    <div class="card section">
+    <div class="card section" id="external-section">
       <h2>External public-source signals</h2>
       <p class="muted">Only external sources are displayed here. Company-owned websites, reports, policies and supplier documents are evidence sources, not external public-source signals.</p>
       <div id="external"></div>
@@ -190,7 +195,7 @@
 function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
 function arr(x){return Array.isArray(x)?x:(x?[x]:[])}
 function setStatus(msg,type='notice'){const el=$('status'); if(!el)return; el.className=type; el.innerHTML=msg}
-async function health(){try{let r=await fetch('/api/health',{cache:'no-store'});let j=await r.json();$('version').textContent='System status: ready · External search: '+((j.tavily_configured||j.google_search_configured)?'configured':'not configured');}catch(e){$('version').textContent='System status: backend unavailable';}}
+async function health(){try{let r=await fetch('/api/health',{cache:'no-store'});let j=await r.json();$('version').textContent='Build '+(j.release||'v68')+' · '+(j.release_date||'2026-07-16')+' · '+j.version+' · External search: '+((j.tavily_configured||j.google_search_configured)?'configured':'not configured');}catch(e){$('version').textContent='Build {{APP_RELEASE_LABEL}} · {{APP_RELEASE_DATE}} · backend health check unavailable';}}
 health();
 $('chooseFile')?.addEventListener('click',()=>$('file').click());
 $('file')?.addEventListener('change',()=>{$('fileName').textContent=$('file').files[0]?.name||'No file selected'});
@@ -198,25 +203,24 @@
 async function scanDoc(){let f=$('file').files[0]; if(!f){setStatus('Please choose a document to upload.','notice warning');return} let b64=await fileToBase64(f); await runScan('/api/scan/document',{filename:f.name,mime_type:f.type,content_base64:b64,company_name:$('docCompanyName').value.trim()});}
 async function runScan(endpoint,payload){$('runUrl').disabled=true;$('runDoc').disabled=true;$('results').classList.add('hidden');setStatus('<span class="spinner"></span> Running scan. This can take a moment while the website and external stakeholder signals are checked.','notice');try{let r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});let text=await r.text();let j;try{j=JSON.parse(text)}catch(_){throw new Error((text.trim().startsWith('<!DOCTYPE')||text.trim().startsWith('<html'))?'The server did not respond in time. This usually happens when the free hosting instance was asleep after a period of inactivity and needed to wake up (this can take up to a minute) on top of the scan itself. Please try the same scan again \u2014 it should complete normally now that the server is awake.':(text.slice(0,250)||'The server returned an unreadable response.'))}if(!r.ok||j.error)throw new Error(j.error||'Scan failed');lastData=j;render(j);setStatus('Scan completed. Review the findings below and use the company report download buttons.','notice');}catch(e){setStatus('Scan failed: '+esc(e.message),'notice error')}finally{$('runUrl').disabled=false;$('runDoc').disabled=false;}}
 function fileToBase64(file){return new Promise((res,rej)=>{let r=new FileReader();r.onload=()=>res(String(r.result).split(',')[1]);r.onerror=rej;r.readAsDataURL(file);});}
-$('runUrl').addEventListener('click',scanUrl);$('runDoc').addEventListener('click',scanDoc);
+$('runUrl').addEventListener('click',scanUrl);$('runDoc').addEventListener('click',scanDoc);$('url').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();scanUrl();}});$('docCompanyName').addEventListener('keydown',e=>{if(e.key==='Enter'&&$('file').files[0]){e.preventDefault();scanDoc();}});
 function render(d){$('results').classList.remove('hidden');$('summary').textContent=d.assessment_summary_specific||d.report?.summary||'';$('methodology').textContent=d.methodology||'';$('disclaimer').textContent=d.disclaimer||'';let rn=$('resolutionNote');if(d.fallback_note){rn.textContent=d.fallback_note;rn.classList.remove('hidden')}else{rn.classList.add('hidden')}let rw=$('reliabilityWarning');if(d.data_reliability_warning){let cd=d.crawl_diagnostics||{};rw.textContent='⚠ Data reliability: '+d.data_reliability_warning+(cd.pages_attempted?` (${cd.pages_failed||0}/${cd.pages_attempted} page fetches failed.)`:'');rw.classList.remove('hidden')}else{rw.classList.add('hidden')}renderScores(d);renderCoverage(d);renderDrivers(d);renderClaims(d);renderRedFlags(d);renderExternal(d);renderActions(d);window.scrollTo({top:$('results').offsetTop-10,behavior:'smooth'});}
 function sourceTypeLabel(item){return item?.source_type||'Reviewed source'}
 function methodLabel(method){let m=String(method||'direct').toLowerCase();if(m==='reader_fallback')return 'Public text fallback';if(m==='uploaded')return 'Uploaded';if(m==='direct_thin')return 'Direct (limited text)';return 'Direct fetch'}
-function sourceRows(items){return arr(items).map(item=>{let method=methodLabel(item.fetch_method);let fallback=String(item.fetch_method||'').toLowerCase()==='reader_fallback';let url=item.url||'';let urlCell=/^https?:\/\//i.test(url)?`<a href="${esc(url)}" target="_blank" rel="noopener">${esc(url)}</a>`:esc(url);return `<tr><td><strong>${esc(item.name||sourceTypeLabel(item))}</strong><br><span class="small">${esc(item.document_type||sourceTypeLabel(item))}</span></td><td class="source-url">${urlCell}</td><td>${esc(item.audience_assessment||'Not assessed')}</td><td><span class="method-badge ${fallback?'fallback':''}">${esc(method)}</span></td></tr>`}).join('')}
+function sourceRows(items){return arr(items).map(item=>{let method=methodLabel(item.fetch_method);let fallback=String(item.fetch_method||'').toLowerCase()==='reader_fallback';let url=item.url||'';let urlCell=/^https?:\/\//i.test(url)?`<a href="${esc(url)}" target="_blank" rel="noopener">${esc(url)}</a>`:esc(url);let status=item.analysis_status||'Retrieved and analysed';let cls=status.includes('partially')?'partial':(status.includes('Limited')||status.includes('not analysed')?'limited':'');let contribution=(item.claim_signal_count||0)+' claim signal(s)'+(arr(item.claim_dimensions).length?' · '+arr(item.claim_dimensions).join(' / '):'');return `<tr><td><strong>${esc(item.name||sourceTypeLabel(item))}</strong><br><span class="small">${esc(item.document_type||sourceTypeLabel(item))}</span></td><td class="source-url">${urlCell}</td><td><span class="status-badge ${cls}">${esc(status)}</span><br><span class="small">${esc(item.characters_analysed||0)} characters analysed · ${esc(contribution)}</span></td><td>${esc(item.audience_assessment||'Not assessed')}</td><td><span class="method-badge ${fallback?'fallback':''}">${esc(method)}</span></td></tr>`}).join('')}
 function renderCoverage(d){let inv=d.scan_inventory||{};let s=inv.summary||{};let pages=arr(inv.website_pages),docs=arr(inv.documents),failed=arr(inv.failed_fetches);if(!pages.length&&!docs.length){let fallback=arr(d.report?.pages_reviewed).map(u=>({name:u,url:u,source_type:String(u).toLowerCase().includes('.pdf')?'Document / PDF':'Website page',document_type:String(u).toLowerCase().includes('.pdf')?'PDF document':'Website page',audience_assessment:'Not assessed',fetch_method:'direct'}));pages=fallback.filter(x=>x.source_type==='Website page');docs=fallback.filter(x=>x.source_type!=='Website page');s={website_pages_reviewed:pages.length,documents_reviewed:docs.length,reviewed_total:fallback.length,domains_reviewed:new Set(fallback.map(x=>{try{return new URL(x.url).hostname}catch(_){return ''}}).filter(Boolean)).size,fetch_failures:d.crawl_diagnostics?.pages_failed||0,fallback_pages:d.crawl_diagnostics?.pages_retrieved_via_fallback||0}}
-let html=`<div class="coverage-metrics"><div class="coverage-metric"><div class="metric-num">${esc(s.website_pages_reviewed||0)}</div><div class="metric-label">Website pages reviewed</div></div><div class="coverage-metric"><div class="metric-num">${esc(s.documents_reviewed||0)}</div><div class="metric-label">Documents / PDFs reviewed</div></div><div class="coverage-metric"><div class="metric-num">${esc(s.domains_reviewed||0)}</div><div class="metric-label">Domains covered</div></div><div class="coverage-metric"><div class="metric-num">${esc(s.fetch_failures||0)}</div><div class="metric-label">Failed fetch attempts</div></div></div>`;
-if(pages.length){html+=`<details class="source-register" open><summary>Website pages reviewed (${pages.length})</summary><div class="source-register-body"><table class="source-table"><thead><tr><th>Page</th><th>URL</th><th>Audience</th><th>Retrieval</th></tr></thead><tbody>${sourceRows(pages)}</tbody></table></div></details>`}
-if(docs.length){html+=`<details class="source-register" open><summary>Documents and PDF reports reviewed (${docs.length})</summary><div class="source-register-body"><table class="source-table"><thead><tr><th>Document</th><th>Source</th><th>Audience</th><th>Retrieval</th></tr></thead><tbody>${sourceRows(docs)}</tbody></table></div></details>`}
+let html=`<div class="coverage-metrics"><div class="coverage-metric"><div class="metric-num">${esc(s.website_pages_reviewed||0)}</div><div class="metric-label">Website pages retrieved</div></div><div class="coverage-metric"><div class="metric-num">${esc(s.documents_reviewed||0)}</div><div class="metric-label">Documents / PDFs retrieved</div></div><div class="coverage-metric"><div class="metric-num">${esc(s.domains_reviewed||0)}</div><div class="metric-label">Domains covered</div></div><div class="coverage-metric"><div class="metric-num">${esc(s.fetch_failures||0)}</div><div class="metric-label">Failed fetch attempts</div></div></div>`;
+if(pages.length){html+=`<details class="source-register" open><summary>Website pages retrieved (${pages.length})</summary><div class="source-register-body"><table class="source-table"><thead><tr><th>Page</th><th>URL</th><th>Analysis status</th><th>Audience</th><th>Retrieval</th></tr></thead><tbody>${sourceRows(pages)}</tbody></table></div></details>`}
+if(docs.length){html+=`<details class="source-register" open><summary>Documents and PDF reports retrieved (${docs.length})</summary><div class="source-register-body"><table class="source-table"><thead><tr><th>Document</th><th>Source</th><th>Analysis status</th><th>Audience</th><th>Retrieval</th></tr></thead><tbody>${sourceRows(docs)}</tbody></table></div></details>`}
 if(failed.length){html+=`<details class="source-register"><summary>Pages attempted but not accessed (${failed.length})</summary><div class="source-register-body"><table class="source-table"><thead><tr><th>Page</th><th>URL</th><th>Status / reason</th><th>Discovery</th></tr></thead><tbody>${failed.map(x=>`<tr><td>${esc(x.name||'Page')}</td><td class="source-url"><span class="failed-source">${esc(x.url||'')}</span></td><td>${esc((x.http_status?'HTTP '+x.http_status+' · ':'')+(x.error||''))}</td><td>${esc(x.discovery_source||'discovered')}</td></tr>`).join('')}</tbody></table></div></details>`}
 html+=`<p class="coverage-note">${esc(inv.note||'Only successfully reviewed sources contribute to the claim analysis. Failed fetch attempts are shown separately.')}${s.fallback_pages?` ${esc(s.fallback_pages)} page(s) required a public text-extraction fallback.`:''}</p>`;$('scanCoverage').innerHTML=html}
-function downloadSourceRegisterCsv(){if(!lastData)return;let inv=lastData.scan_inventory||{},rows=[['Status','Source type','Name','URL / source','Domain','Document type','Audience','EmpCo relevance','Fetch method','Discovery source','Characters extracted']];for(let item of [...arr(inv.website_pages),...arr(inv.documents)])rows.push(['Reviewed',item.source_type||'',item.name||'',item.url||'',item.domain||'',item.document_type||'',item.audience_assessment||'',item.empco_relevance||'',item.fetch_method||'',item.discovery_source||'',item.characters_extracted||0]);for(let item of arr(inv.failed_fetches))rows.push(['Not accessed','Failed fetch',item.name||'',item.url||'','',item.error||'','','','failed',item.discovery_source||'',0]);let csv=rows.map(row=>row.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\r\n');let blob=new Blob([csv],{type:'text/csv;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='durably_scan_source_register.csv';document.body.appendChild(a);a.click();URL.revokeObjectURL(a.href);a.remove()}
+function downloadSourceRegisterCsv(){if(!lastData)return;let inv=lastData.scan_inventory||{},rows=[['Status','Source type','Name','URL / source','Domain','Document type','Audience','EmpCo relevance','Fetch method','Discovery source','Characters extracted','Characters analysed','Claim signals','Claim dimensions']];for(let item of [...arr(inv.website_pages),...arr(inv.documents)])rows.push(['Reviewed',item.source_type||'',item.name||'',item.url||'',item.domain||'',item.document_type||'',item.audience_assessment||'',item.empco_relevance||'',item.fetch_method||'',item.discovery_source||'',item.characters_extracted||0,item.characters_analysed||0,item.claim_signal_count||0,arr(item.claim_dimensions).join(' / ')]);for(let item of arr(inv.failed_fetches))rows.push(['Not accessed','Failed fetch',item.name||'',item.url||'','',item.error||'','','','failed',item.discovery_source||'',0,0,0,'']);let csv=rows.map(row=>row.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\r\n');let blob=new Blob([csv],{type:'text/csv;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='durably_scan_source_register.csv';document.body.appendChild(a);a.click();URL.revokeObjectURL(a.href);a.remove()}
 function scoreInterp(n){
   if(n==null)return '';
-  if(n>=85)return 'Very high — multiple severe signals';
-  if(n>=70)return 'High — priority review needed';
-  if(n>=50)return 'Elevated — several claim signals';
-  if(n>=30)return 'Moderate — some wording needs review';
-  return 'Low — limited claim-risk signal';
+  if(n>=90)return 'Very high — multiple severe signals';
+  if(n>=75)return 'High — priority review needed';
+  if(n>=45)return 'Medium — material claim signals require review';
+  return 'Low — limited detected claim-risk signal';
 }
 function renderScores(d){
   let scores=[['Global',d.global_score??d.overall_score,d.global_risk??d.overall_risk,'global'],
@@ -303,7 +307,7 @@
 function renderRedFlags(d){let by=d.red_flags_by_dimension||{};let green=arr(by.green);let social=arr(by.social);let flags=arr(d.stakeholder_red_flags);let regulatory=[];let external=[];for(let f of flags){let s=String(typeof f==='string'?f:JSON.stringify(f));let l=s.toLowerCase();if(l.includes('empco')||l.includes('regulation')||l.includes('forced labour')||l.includes('forced labor'))regulatory.push(s);if(l.includes('external')||l.includes('public-source')||l.includes('contradict'))external.push(s)}
 function isUrgent(s){let l=s.toLowerCase();return l.includes('high')||l.includes('empco')||l.includes('forced labour')||l.includes('forced labor')||l.includes('regulation')}
 function box(cls,title,items){items=arr(items).filter(Boolean);return `<div class="redflag ${cls}"><h3>${esc(title)}</h3><ul>${items.length?items.slice(0,6).map(x=>isUrgent(x)?`<li class="urgent">&#9888; ${esc(x)}</li>`:`<li>${esc(x)}</li>`).join(''):'<li>No specific red flag retained.</li>'}</ul></div>`}$('redflags').innerHTML=`<div class="redflag-grid">${box('green-flag','Green-claim red flags',green)}${box('social-flag','Social-claim red flags',social)}${box('reg-flag','Regulatory red flags',regulatory)}${box('', 'Negative external-context red flags', external)}</div>`;}
-function renderExternal(d){let gLayer=d.external_research?.green||{},sLayer=d.external_research?.social||{};let g=arr(gLayer.targeted_negative_sources),sigs=arr(sLayer.targeted_negative_sources),legacy=arr(d.external_research?.targeted_negative_sources);let all=[...g,...sigs,...legacy];let seen=new Set();all=all.filter(x=>{let k=x.url||x.title;if(!k||seen.has(k))return false;seen.add(k);return true});function card(src){let url=src.url||'',source=src.source_name||String(url).replace(/^https?:\/\//,'').split('/')[0]||src.category||'External source';return `<div class="source" style="border:1px solid var(--line);border-left:4px solid var(--danger);border-radius:11px;padding:12px;margin:9px 0;background:#fff"><div style="display:flex;justify-content:space-between;gap:12px"><strong>${esc(src.title||'External stakeholder signal')}</strong><span class="badge">${esc(src.review_status||'Retained — manual verification required')}</span></div><div class="claim-summary-line"><span>${esc(source)}</span><span>· ${esc(src.published_date||'Date not available')}</span><span>· ${esc(src.status||'Status unclear')}</span><span>· ${esc(src.relevance||'Medium')} relevance</span></div>${src.content?`<p style="margin:7px 0 4px">${esc(src.content)}</p>`:''}<div class="small"><strong>Related claim area:</strong> ${esc(src.related_claim_area||src.dimension||'Sustainability claims')} · <strong>Entity match:</strong> ${esc(src.entity_match||'Direct')}</div>${url?`<a href="${esc(url)}" target="_blank" rel="noopener">Open source</a>`:''}</div>`}let note='<div class="stakeholder-note"><strong>External context:</strong> candidate search results are filtered for company ownership, entity match, negative polarity and green/social relevance. Only retained signals are shown. They require manual verification before use.</div>';let diags=[gLayer.search_diagnostics,sLayer.search_diagnostics].filter(Boolean);let diagHtml='';if(diags.length){let raw=diags.reduce((n,x)=>n+Number(x.raw_result_count||0),0),matched=diags.reduce((n,x)=>n+Number(x.company_matched_count||0),0),negative=diags.reduce((n,x)=>n+Number(x.negative_candidate_count||0),0),retained=diags.reduce((n,x)=>n+Number(x.retained_count||0),0),competitors=diags.reduce((n,x)=>n+Number(x.competitor_primary_rejected_count||0),0),fallback=diags.some(x=>x.fallback_used);diagHtml=`<div class="small" style="margin:7px 0;color:var(--muted)"><strong>Search diagnostics:</strong> ${raw} raw result(s) · ${matched} direct company match(es) · ${negative} negative candidate(s) · ${retained} retained${competitors?` · ${competitors} competitor-primary result(s) excluded`:''}${fallback?' · fallback stakeholder searches used':''}.</div>`}let empty='<p class="empty">No negative external public-source signal was retained. If external search was unavailable or returned incomplete results, this does not confirm an absence of relevant external criticism or enforcement.</p>';$('external').innerHTML=note+diagHtml+(all.length?all.slice(0,6).map(card).join(''):empty);}
+function renderExternal(d){let gLayer=d.external_research?.green||{},sLayer=d.external_research?.social||{};let g=arr(gLayer.targeted_negative_sources),sigs=arr(sLayer.targeted_negative_sources),legacy=arr(d.external_research?.targeted_negative_sources);let all=[...g,...sigs,...legacy];let seen=new Set();all=all.filter(x=>{let k=x.url||x.title;if(!k||seen.has(k))return false;seen.add(k);return true});function card(src){let url=src.url||'',source=src.source_name||String(url).replace(/^https?:\/\//,'').split('/')[0]||src.category||'External source';return `<div class="source" style="border:1px solid var(--line);border-left:4px solid var(--danger);border-radius:11px;padding:12px;margin:9px 0;background:#fff"><div style="display:flex;justify-content:space-between;gap:12px"><strong>${esc(src.title||'External stakeholder signal')}</strong><span class="badge">${esc(src.review_status||'Retained — manual verification required')}</span></div><div class="claim-summary-line"><span>${esc(source)}</span><span>· ${esc(src.published_date||'Date not available')}</span><span>· ${esc(src.status||'Status unclear')}</span><span>· ${esc(src.relevance||'Medium')} relevance</span></div>${src.content?`<p style="margin:7px 0 4px">${esc(src.content)}</p>`:''}<div class="small"><strong>Related claim area:</strong> ${esc(src.related_claim_area||src.dimension||'Sustainability claims')} · <strong>Entity match:</strong> ${esc(src.entity_match||'Direct')}</div>${url?`<a href="${esc(url)}" target="_blank" rel="noopener">Open source</a>`:''}</div>`}let note='<div class="stakeholder-note"><strong>Verification status:</strong> Candidate = automatically discovered but not retained; Retained = entity match and negative relevance passed automated checks; Verified = manually confirmed. Only retained and verified signals are shown and may affect context. Automated retained signals still require human verification before external use.</div>';let diags=[gLayer.search_diagnostics,sLayer.search_diagnostics].filter(Boolean);let diagHtml='';if(diags.length){let raw=diags.reduce((n,x)=>n+Number(x.raw_result_count||0),0),matched=diags.reduce((n,x)=>n+Number(x.company_matched_count||0),0),negative=diags.reduce((n,x)=>n+Number(x.negative_candidate_count||0),0),retained=diags.reduce((n,x)=>n+Number(x.retained_count||0),0),competitors=diags.reduce((n,x)=>n+Number(x.competitor_primary_rejected_count||0),0),fallback=diags.some(x=>x.fallback_used);diagHtml=`<div class="small" style="margin:7px 0;color:var(--muted)"><strong>Search diagnostics:</strong> ${raw} raw result(s) · ${matched} direct company match(es) · ${negative} negative candidate(s) · ${retained} retained${competitors?` · ${competitors} competitor-primary result(s) excluded`:''}${fallback?' · fallback stakeholder searches used':''}.</div>`}let empty='<p class="empty">No negative external public-source signal was retained. If external search was unavailable or returned incomplete results, this does not confirm an absence of relevant external criticism or enforcement.</p>';$('external').innerHTML=note+diagHtml+(all.length?all.slice(0,6).map(card).join(''):empty);}
 function renderActions(d){let actions=arr(d.company_action_plan);$('actions').innerHTML=actions.length?actions.map(a=>{let p=String(a.priority||'');let urgent=/priority\s*1\b/i.test(p);return `<div class="action-card${urgent?' urgent-action':''}"><strong class="${urgent?'urgent-priority':''}">${urgent?'&#9888; ':''}${esc(p)}</strong> ${esc(a.title||'')}<br><span class="muted">${esc(a.action||'')}</span></div>`}).join(''):'<p class="empty">No actions available.</p>'}
 function miniSource(r){return (r.source_label||r.source_url||'Reviewed material').replace(/\s+/g,' ').slice(0,90)}
 async function downloadCompanyReportPdf(){
